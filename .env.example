# OpenClaw Environment Variables
# Copia este ficheiro para .env e preenche os valores

# === OBRIGATÓRIO ===

# Token do bot Telegram (obter via @BotFather)
TELEGRAM_BOT_TOKEN=

# API Key da Anthropic (https://console.anthropic.com/)
ANTHROPIC_API_KEY=

# Password para o setup wizard do Railway
SETUP_PASSWORD=

# Token de autenticação do Gateway (gerar com: openssl rand -base64 32)
OPENCLAW_GATEWAY_TOKEN=

# === RAILWAY CONFIG ===

PORT=8080
OPENCLAW_STATE_DIR=/data/.openclaw
OPENCLAW_WORKSPACE_DIR=/data/workspace
TZ=Europe/Lisbon

# === OPCIONAL ===

# Anthropic Base URL (só se usares proxy)
# ANTHROPIC_BASE_URL=https://api.anthropic.com

# OpenRouter (alternativa ao Anthropic)
# OPENROUTER_API_KEY=

# Log level (debug, info, warn, error)
# LOG_LEVEL=info
